'use client';

export default function InboxPage() {
  return (
    <div style={{ padding: '40px', textAlign: 'center' }}>
      <h1 className="heading-section">Inbox</h1>
      <p style={{ color: 'var(--text-secondary)', marginTop: '16px' }}>Your notifications and updates will appear here.</p>
    </div>
  );
}
